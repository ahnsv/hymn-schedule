extends layout

block content
  h1= title
  p Welcome to #{title}
  form
    fieldset(style="display: flex")
      legend Schedule Add
      label title
      input(name="title")

      label.range--start start
      input(name="start")

      label.range--end end
      input(name="end")

      input(type="submit", value="submit")


